# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## プロジェクト概要

Astro 5.x で構築されたエンジニアのポートフォリオサイトです。純粋な Astro コンポーネント（React/Vue/Svelte なし）を使用し、TypeScript は strict モードで動作します。

## 開発コマンド

```bash
# 開発
pnpm dev              # localhost:4321 で開発サーバーを起動
pnpm build            # 本番用サイトを ./dist/ にビルド
pnpm preview          # 本番ビルドをローカルでプレビュー

# コード品質
pnpm lint             # すべての .js/.ts/.astro ファイルに対して ESLint チェックを実行
pnpm lint:fix         # ESLint の問題を自動修正
pnpm format           # Prettier でコードをフォーマット

# Astro CLI
pnpm astro check      # TypeScript の型チェックを実行
pnpm astro add        # Astro インテグレーションを追加
```

## ページ構成と設計意図

### `/` - トップページ
**目的**: 訪問者に全体像を素早く伝え、詳細ページへ誘導する

**コンテンツ構成**:
- Hero セクション
- スキル概要（短く、キーとなるスキルのみ）
- プロジェクトのハイライト（代表的なプロジェクト数件）
- About / Projects への明確な導線

**実装時の注意**: 情報量を抑え、詳細は各専門ページに誘導する設計

### `/about` - エンジニアとしての背景・価値観
**目的**: 人となりと技術的アプローチを伝える

**コンテンツ構成**:
- 技術的バックグラウンド
- 問題解決に対するスタンス
- 現在の取り組み・関心領域
- Contact 情報（SNS・メール）

**設計判断**: Contact を独立ページではなく About に統合することで、人となりと連絡先を一箇所で提示

### `/skills` - 技術スキルまとめ
**目的**: 保有スキルを体系的に整理して提示

**コンテンツ構成**:
- フロントエンド技術
- バックエンド技術
- インフラ・DevOps
- 認証・セキュリティ
- API 設計
- LLM・自動化

**実装時の注意**: カテゴリ整理が前提。視覚的に分かりやすい構造が必要

### `/projects` - プロジェクト一覧
**目的**: 手がけたプロジェクトを一覧し、興味のあるものを選択できるようにする

**コンテンツ構成**:
- プロジェクトカード（サムネイル、概要、技術スタック）
- タグによるフィルタリング（技術・領域別）
- 各プロジェクトの詳細ページへのリンク

### `/projects/[slug]` - プロジェクト詳細
**目的**: 個別プロジェクトの深い理解を提供

**コンテンツ構成**:
- プロジェクト概要
- 背景・目的（なぜ作ったのか）
- 技術選定理由（なぜその技術を選んだのか）
- 設計・アーキテクチャの工夫（どう実装したのか）
- 成果・学び

**実装時の注意**: 動的ルーティング `[slug]` を使用。将来的にコンテンツコレクションでの管理を想定

## アーキテクチャ

### ルーティング
- **ファイルベースルーティング**: `src/pages/` 内のファイルが自動的にルートになる
- 動的ルート `/projects/[slug]` は今後実装予定
- すべてのページは `src/layouts/Layout.astro` でラップされ、一貫した HTML 構造を提供

### レイアウトパターン
- 単一のマスターレイアウト: `src/layouts/Layout.astro`
- HTML ラッパーとグローバルベースラインスタイルのみを提供
- ページは `<slot />` タグを介してコンテンツを注入

### コンポーネント構造
- すべてのコンポーネントは純粋な Astro（`.astro` ファイル）
- フロントマター構文: ファイル上部の `---` デリミタ間にコードを記述
- コンポーネントスコープのスタイルは `<style>` タグを使用（Layout 外にグローバル CSS なし）
- アセットはモジュールとしてインポート: `import logo from '../assets/logo.svg'`、`logo.src` で使用

### TypeScript 設定
- Astro の **strict プリセット**を使用（`astro/tsconfigs/strict`）
- コンポーネントフロントマターでの明示的な型付けが必要
- `.astro/types.d.ts` に自動生成される型定義
- 現在、独立した `.ts` ファイルはなし - すべてのロジックは `.astro` コンポーネント内

## プロジェクトの状態

- ほとんどのページにはプレースホルダーコンテンツが含まれている（上記ページ構成に沿って実装予定）
- `/projects/[slug]` の動的ルーティングは未実装
- コンテンツコレクションやデータ管理の仕組みはまだ導入していない
- 静的出力のみ（SSR は未設定）
- `z/`ディレクトリは、AIエージェント専用のディレクトリです。複数のステップを必要とするタスクは、ここに実行計画を書き出し、実施ステータスをチェックしながら進めてください。